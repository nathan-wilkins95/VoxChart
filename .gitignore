# Python
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
*.egg-info/

# PyInstaller
dist/
build/
*.spec

# App data
chart_notes/
training_corpus/
medical_terms.db

# Installer build
installer_build/

# Model cache
.cache/
*.bin
*.pt

installer/output/*.exe

# Code signing - NEVER commit private keys
signing/*.pfx
signing/*.p12
signing/*.key
signing/*.pem

# Logs
logs/
*.log

# Runtime markers
.shortcut_created

# Epic FHIR — token cache (contains access tokens, never commit)
epic_fhir_tokens.json

# Epic exports (local patient data, never commit)
epic_exports/
